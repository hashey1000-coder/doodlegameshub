// useKidsMode is now a thin re-export of the shared context.
// All components calling this hook share the same Kids Mode state.
export { useKidsModeContext as useKidsMode } from "@/contexts/KidsModeContext";
